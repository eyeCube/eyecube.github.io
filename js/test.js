/*
 *  testing
 *  No code from this file should be executed in
 *  normal running of the program.
 */


//TESTING
/*
var en = Things.create_actor('a', 15, 15);
en.act = function () {
    console.log("a's turn");
    this.port(this.x + 1, this.y);
    Game.scheduler.setDuration(2 * AVG_SPEED);
}
*/
// UBER TESTING
let fname = `Jaek`;
let lname = "Wartonne";
console.log(`${fname} hhh ${lname}`);

function d(strings, ...values) {
    console.log(strings);
    console.log(values);
}
d`billlibong!`;

var arr = [1, 2, 3, 4, 5];

arr.forEach((value, index) => {
    console.log(value, index);
})

function getRect(son) {
    rec(son);
}
function getSum(bitch) {
    return sum(bitch);
}

//////




